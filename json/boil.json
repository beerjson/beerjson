{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/beerjson/beerjson/main/json/boil.json",
  "type": "object",
  "additionalProperties": false,
  "$defs": {
    "BoilProcedureType": {
      "type": "object",
      "description": "BoilProcedureType defines the procedure for performing a boil. A boil procedure with no steps is the same as a standard single step boil.",
      "additionalProperties": false,
      "properties": {
        "name": {
          "description": "Name of the boil procedure.",
          "type": "string"
        },
        "description": {
          "description": "Free text description of the boil procedure.",
          "type": "string"
        },
        "notes": {
          "description": "Free text notes about the boil procedure.",
          "type": "string"
        },
        "pre_boil_size": {
          "$ref": "measureable_units.json#/$defs/VolumeType"
        },
        "boil_time": {
          "$ref": "measureable_units.json#/$defs/TimeType"
        },
        "boil_steps": {
          "description": "The steps that make up the boil, in the order they are performed.",
          "type": "array",
          "items": {
            "$ref": "boil_step.json#/$defs/BoilStepType"
          }
        }
      },
      "required": [
        "boil_time"
      ]
    }
  }
}
