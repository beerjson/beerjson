{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/beerjson/beerjson/main/json/fermentation.json",
  "type": "object",
  "additionalProperties": false,
  "$defs": {
    "FermentationProcedureType": {
      "type": "object",
      "description": "FermentationProcedureType defines the procedure for performing fermentation.",
      "additionalProperties": false,
      "properties": {
        "name": {
          "description": "Name of the fermentation procedure.",
          "type": "string"
        },
        "description": {
          "description": "Free text description of the fermentation procedure.",
          "type": "string"
        },
        "notes": {
          "description": "Free text notes about the fermentation procedure.",
          "type": "string"
        },
        "fermentation_steps": {
          "description": "The steps that make up the fermentation, in the order they are performed.",
          "type": "array",
          "minItems": 1,
          "items": {
            "$ref": "fermentation_step.json#/$defs/FermentationStepType"
          }
        }
      },
      "required": [
        "name",
        "fermentation_steps"
      ]
    }
  }
}
